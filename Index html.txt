<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AI 打地鼠挑戰賽</title>
    <style>
        body {
            background-color: #8BC34A;
            font-family: 'Arial Black', sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            margin: 0;
        }
        h1 { color: #5d4037; text-shadow: 2px 2px #fff; }
        .score-board { font-size: 2rem; color: #fff; margin-bottom: 10px; }
        
        .game-grid {
            display: grid;
            grid-template-columns: repeat(3, 160px);
            grid-gap: 20px;
        }
        
        .hole {
            width: 160px;
            height: 120px;
            background: #5d4037;
            border-radius: 50% 50% 20% 20%;
            position: relative;
            overflow: hidden;
            border: 4px solid #3e2723;
        }
        
        .mole {
            width: 100%;
            height: 100%;
            background: url('https://img.icons8.com/color/120/000000/mole.png') no-repeat center bottom;
            background-size: 70%;
            position: absolute;
            top: 100%; /* 預設藏在地洞裡 */
            transition: top 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            cursor: pointer;
        }
        
        .hole.up .mole {
            top: 10%;
        }
        
        button {
            padding: 15px 30px;
            font-size: 1.5rem;
            background-color: #FF5722;
            color: white;
            border: none;
            border-radius: 10px;
            cursor: pointer;
            box-shadow: 0 4px #bf360c;
            margin-bottom: 20px;
        }
        
        button:active {
            box-shadow: 0 2px #bf360c;
            transform: translateY(2px);
        }
    </style>
</head>
<body>

    <h1>打地鼠挑戰賽!</h1>
    <div class="score-board">目前得分: <span id="score">0</span></div>
    <button id="start-btn">開始遊戲!</button>

    <div class="game-grid">
        <div class="hole"><div class="mole"></div></div>
        <div class="hole"><div class="mole"></div></div>
        <div class="hole"><div class="mole"></div></div>
        <div class="hole"><div class="mole"></div></div>
        <div class="hole"><div class="mole"></div></div>
        <div class="hole"><div class="mole"></div></div>
    </div>

    <script>
        const holes = document.querySelectorAll('.hole');
        const scoreBoard = document.querySelector('#score');
        const moles = document.querySelectorAll('.mole');
        const startBtn = document.querySelector('#start-btn');
        let lastHole;
        let timeUp = false;
        let score = 0;

        function randomTime(min, max) {
            return Math.round(Math.random() * (max - min) + min);
        }

        function randomHole(holes) {
            const idx = Math.floor(Math.random() * holes.length);
            const hole = holes[idx];
            if (hole === lastHole) return randomHole(holes);
            lastHole = hole;
            return hole;
        }

        function peep() {
            const time = randomTime(600, 1200);
            const hole = randomHole(holes);
            hole.classList.add('up');
            setTimeout(() => {
                hole.classList.remove('up');
                if (!timeUp) peep();
            }, time);
        }

        function startGame() {
            scoreBoard.textContent = 0;
            timeUp = false;
            score = 0;
            startBtn.disabled = true;
            peep();
            setTimeout(() => {
                timeUp = true;
                startBtn.disabled = false;
                alert('時間到！你的最終得分是：' + score);
            }, 15000); // 遊戲時間 15 秒
        }

        function bonk(e) {
            if(!e.isTrusted) return; // 防止腳本模擬點擊
            score++;
            this.parentNode.classList.remove('up');
            scoreBoard.textContent = score;
        }

        moles.forEach(mole => mole.addEventListener('click', bonk));
        startBtn.addEventListener('click', startGame);
    </script>
</body>
</html>